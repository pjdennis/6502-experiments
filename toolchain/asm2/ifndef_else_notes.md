Why are we using X in process_ifndef?
Can most of the processing code be shared between ifdef and ifndef - just use a flag to flip the decision logic?
Is there a test for the ifdef nesting limit?
Use an explicit limit constant for the else seen array
